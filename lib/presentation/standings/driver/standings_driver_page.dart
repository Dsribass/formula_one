import 'package:flutter/material.dart';

class StandingsDriverPage extends StatelessWidget {
  const StandingsDriverPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Drivers'),
    );
  }
}
